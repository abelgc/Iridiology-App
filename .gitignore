# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# git worktrees
.worktrees/
.env*.local

# integration tests: .env.test is deliberately NOT ignored.
# The `.env*` rule above would have swallowed it. It carries no secret — only the Supabase
# CLI's fixed local development keys, which are identical on every machine, printed by
# `npx supabase start`, and published in Supabase's own docs. Committing it is what lets a
# fresh clone run `npm run test:int` without hunting for values, and src/test/integration-setup.ts
# refuses to start unless the URL in it points at 127.0.0.1:54321.
!.env.test
